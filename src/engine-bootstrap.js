// Global UI primitives exposed by the engine bundle (loaded before this file).
const {
  Router,
  html,
  Toast,
  AnimUtils,
  BottomSheet,
  utils,
  ShakeIt,
  ToggleButton,
  Modal,
  DatePicker,
} = HMI_UI;